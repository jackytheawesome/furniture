# Скопировать на сервер: cp .env.production.example .env.production

POSTGRES_USER=furniture
POSTGRES_PASSWORD=замените-на-длинный-пароль
POSTGRES_DB=furniture

# openssl rand -base64 32
AUTH_SECRET=замените-на-случайную-строку

# Для nginx и certbot (не читается контейнерами, только в инструкции)
DOMAIN=example.ru
